# ==============================================================================
# COMMAND MAPPING INDEX (Format: [cmd] ---> [intents])
# ==============================================================================

# ==============================================================================
# 1: PROJECTS INIT & OPENCODE BRIDGE
# ==============================================================================

# --- OpenCode Direct Terminal Launcher ---
~/.config/local-ai/opencode-bridge/opencode-bridge ---> ocb, opencode bridge

# --- Projects ---
~/Projects/qwen-hypr ---> projects qwen, projects
~/Projects/quickshell ---> projects quickshell, projects


# ==============================================================================
# 2: AGENTIC TOOLS
# ==============================================================================

# --- System Info & Diagnostics ---
[TOOL] date ---> time date, show current date
[TOOL] date; cal -s ---> calendar date, show calendar, what is today's date
[TOOL] df -h / ---> disk usage, nvme drive usage, check storage space
[TOOL] ~/.config/local-ai/ai-suggestion/tools/ai-system-diagnosis ---> system health, system diagnosis, why is my system slow
[TOOL] ~/.config/local-ai/ai-suggestion/tools/update-inspector ---> update inspector
[TOOL] ~/.config/local-ai/ai-suggestion/tools/ai-log-checker ---> ai log, system crashed, check errors, events

# --- Hyprland Integration ---
[TOOL] hyprctl clients ---> hyprland clients, list open windows, show active apps

# --- Weather ---
[TOOL] curl -s "wttr.in/?format=3" ---> weather simple, rain forcast simple
[TOOL] curl -s wttr.in ---> weather full, rain forcast full

# --- GUI Navigation ---
[TOOL] xdg-open ~/Documents ---> open documents


# ==============================================================================
# 3: COMMANDS & SCRIPTS
# ==============================================================================

# --- Applications & App Launcher ---
~/.config/local-ai/app-launcher/app-launcher.py ---> app, app launcher, launcher

# --- Interactive Shell Aliases & Directory Navigation ---
btop ---> memory, cpu
cd ~/Documents ---> cd documents, go to documents, open documents

# --- Applications (GUI & Webapps) ---
omarchy-launch-webapp https://music.youtube.com/ ---> youtube music, yt music, youtube webapp
uwsm app -- brave-origin-beta --user-data-dir="~/.config/brave-spotify-bunker" --app=https://open.spotify.com/ >/dev/null 2>&1 ---> spotify music

# --- TUI (Terminal User Interface) Scripts ---
~/.config/local-ai/basepage-tui/basepage.py ---> base page, basepage tui, rss
~/.config/local-ai/basepage-tui/basetracker.py ---> base tracker, basetracker tui

~/.config/local-ai/media-tui/media.py ---> media, cliamp
~/.config/local-ai/media-tui/media.sh ---> media sh
~/.config/local-ai/media-tui/media.lua ---> media lua

~/.config/local-ai/ai-summary/llmsum.py ---> llmsum, ytsum, summary

# --- State & Workflow Management ---
~/.config/local-ai/ai-suggestion/tools/hyprstate/work ---> hyprstate work, workspace work
~/.config/local-ai/ai-suggestion/tools/hyprstate/clean ---> hyprstate clean, workspace clean
~/.config/local-ai/ai-suggestion/tools/hyprstate/media ---> hyprstate media, workspace media

# --- Server Lifecycle Management ---
~/.config/local-ai/ai-suggestion/tools/kill-ai-servers ---> stop server, stop llama server, killserver
